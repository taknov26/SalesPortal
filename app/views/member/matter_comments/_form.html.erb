<%= form_with model: matter_comment, url: member_matter_matter_comments_path(matter) do |f| %>
  <%= f.text_area :comment, class: "form-control" %>
  <%= f.submit "投稿", class: "btn btn-info mt-3 btn-sm" %>
<% end %>
